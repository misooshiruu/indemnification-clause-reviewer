# Optional. The app reads backend settings from the UI, not from env vars — these
# are only defaults/notes for local development.

# Default Ollama endpoint (used if you leave the URL field blank in the UI).
OLLAMA_URL=http://localhost:11434

# API keys are entered in the UI and sent per-request; they are NOT read from env
# and are never stored server-side.
